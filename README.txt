g++ '-Wl,--stack,0x20000000' main.cpp was used for the large input otherwise it stops roughly 730 nodes deep
the basic g++ main.cpp command will properly compile for smaller inputs
with the standard ./a.exe to run it for both cases
